# Pharma-Safe Lens - Backend Dependencies
# PHASE 0 - CPU-only libraries (NO GPU, NO MODELS yet)

# Core framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# OCR (CPU-only) - Phase 1
# Using EasyOCR for Windows compatibility (CPU-only)
# Note: Pillow must be <10.0.0 for EasyOCR compatibility
easyocr==1.7.0
pytesseract==0.3.10
Pillow<10.0.0
opencv-python==4.9.0.80

# Data handling
python-multipart==0.0.6
aiofiles==23.2.1

# Utilities
python-dotenv==1.0.0
levenshtein>=0.21.1  # Fuzzy matching for OCR noise tolerance

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0

# MedGemma and AI/ML (Phase 6 - Real Model on Kaggle GPU)
# GPU-optimized for Tesla T4 x2 on Kaggle
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional: Flash Attention 2 for faster inference (auto-installed on Kaggle)
# flash-attn>=2.0.0  # Uncomment if needed

# NOTE: These packages are installed on Kaggle with GPU T4 x2
# For local development, mock inference is used automatically
